# Этап 7 — адаптив и CSS по папкам

Откройте `index.html` (для JSON-карточек — локальный сервер, как на этапе 5).

Стили: `css/index.css` собирает блоки из `css/blocks/*/`. Адаптив — внутри каждого блока.

Проверьте ширины 1280 / 1024 / 768 / 375.
