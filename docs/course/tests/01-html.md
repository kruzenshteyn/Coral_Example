# Тест. HTML (30)

1. Для чего нужен `<!DOCTYPE html>`?
   - A) Подключает CSS
   - B) Сообщает браузеру режим стандарта HTML5
   - C) Создаёт базу данных
   - D) Включает JavaScript
2. Атрибут `lang` у `<html>` влияет на:
   - A) Только цвет фона
   - B) Язык для скринридеров и перевода
   - C) Скорость DNS
   - D) Версию PHP
3. Тег `<header>` означает:
   - A) Обязательно шапку сайта и больше ничего
   - B) Вводный блок страницы или секции
   - C) Только логотип
   - D) HTTP-заголовок ответа
4. Чем `<article>` отличается от `<div>`?
   - A) Ничем
   - B) Article — самостоятельный фрагмент смысла
   - C) Div запрещён в HTML5
   - D) Article нельзя стилизовать
5. Ссылку на другую страницу размечают:
   - A) `<a href="...">`
   - B) `<link href="...">` в body как текст
   - C) `<button href>`
   - D) `<nav src>`
6. Отправку формы обычно запускает:
   - A) `<button type="submit">` или input submit
   - B) `<a type="submit">`
   - C) `<p type="form">`
   - D) `<img type="submit">`
7. Зачем `for` у `<label>` совпадает с `id` поля?
   - A) Чтобы клик по подписи фокусировал поле
   - B) Для CSS Grid
   - C) Для SQL JOIN
   - D) Это необязательно и ни на что не влияет
8. Атрибут `alt` у `<img>`:
   - A) Запасной текст, если нет картинки, и для a11y
   - B) Адрес картинки
   - C) Ширина в см
   - D) Цвет рамки
9. На странице с точки зрения доступности заголовков:
   - A) Можно сколько угодно `h1` без иерархии
   - B) Иерархия уровней должна быть логичной, обычно один главный `h1`
   - C) Заголовки запрещены
   - D) Только `h6`
10. Список навигационных ссылок логично класть в:
    - A) `<nav>` + список
    - B) Только `<table>`
    - C) `<script>`
    - D) `<meta>`
11. `<section>` стоит использовать, когда:
    - A) Нужен тематический блок с заголовком
    - B) Нужен просто отступ
    - C) Нужен HTTP POST
    - D) Нужен индекс БД
12. Поле пароля:
    - A) `type="password"`
    - B) `type="hidden"` всегда
    - C) `type="text"` обязателен
    - D) Отдельный тег `<password>`
13. `required` в форме:
    - A) Браузер может не дать отправить пустое поле
    - B) Шифрует канал
    - C) Пишет в SQLite
    - D) Отключает CSS
14. `charset` в документе задают, чтобы:
    - A) Корректно читать Unicode (кириллица и т.д.)
    - B) Выбрать порт
    - C) Включить Grid
    - D) Создать cookie
15. Viewport meta нужен в основном для:
    - A) Корректной ширины на мобильных
    - B) SEO-карты сайта
    - C) SMTP
    - D) Git
16. Пустой элемент вроде `<img>` называют:
    - A) Void / empty element
    - B) Live node
    - C) Primary key
    - D) Middleware
17. `<button>` внутри формы без `type`:
    - A) Часто ведёт себя как submit
    - B) Всегда reset
    - C) Нельзя
    - D) Это ссылка
18. Скрыть блок от всех, включая скринридер, атрибутом:
    - A) `hidden`
    - B) `alt=""`
    - C) `rel="nofollow"`
    - D) `method="get"`
19. `<main>` на странице:
    - A) Обычно один, основное содержимое
    - B) Обязательно десять
    - C) Заменяет `<html>`
    - D) Только в email
20. Таблицу данных размечают:
    - A) `<table>`, `<th>`, `<td>`
    - B) Только `<div>` с пробелами
    - C) `<ul>` с запятыми
    - D) JSON-LD обязательно
21. `autocomplete="email"` помогает:
    - A) Браузеру подставить сохранённый email
    - B) Валидировать DNS
    - C) Ускорить GPU
    - D) Заменить HTTPS
22. Элемент `<footer>`:
    - A) Может быть у страницы и у article
    - B) Только один на весь интернет
    - C) Запрещён в HTML5
    - D) Это CSS-свойство
23. Разница `<strong>` и `<b>`:
    - A) strong несёт важность, b — визуальное выделение без обязательного смысла
    - B) Их нельзя использовать
    - C) b семантически сильнее
    - D) Это одно и то же по спецификации смысла
24. `action` у формы:
    - A) URL, куда уйдут данные
    - B) Цвет кнопки
    - C) Тип заголовка
    - D) Имя cookie обязательно
25. Вложенность `<p>` внутри `<p>`:
    - A) Невалидна, браузер закроет первый p
    - B) Обязательна
    - C) Создаёт iframe
    - D) Включает JS
26. Атрибут `disabled` у input:
    - A) Поле не редактируется и обычно не уходит в submit
    - B) Удаляет поле из HTML-файла
    - C) Делает type=hidden
    - D) Кэширует изображение
27. Скринридер озвучит подпись поля, если:
    - A) Есть связанный label (или aria-label)
    - B) Есть только placeholder
    - C) Есть только name
    - D) Есть только CSS content
28. `<link rel="stylesheet">` ставят обычно:
    - A) В `<head>`
    - B) Внутри `<p>`
    - C) Вместо doctype
    - D) В cookie
29. Цель `id` в документе:
    - A) Уникальный якорь/связь на странице
    - B) Повторять на каждом div одинаково
    - C) Заменить class полностью
    - D) Это SQL PRIMARY KEY браузера
30. Валидатор HTML проверяет:
    - A) Соответствие разметки правилам языка
    - B) Цвет кнопок
    - C) Скорость SQL JOIN
    - D) Наличие Docker

## Ключ

1-B 2-B 3-B 4-B 5-A 6-A 7-A 8-A 9-B 10-A 11-A 12-A 13-A 14-A 15-A 16-A 17-A 18-A 19-A 20-A 21-A 22-A 23-A 24-A 25-A 26-A 27-A 28-A 29-A 30-A
